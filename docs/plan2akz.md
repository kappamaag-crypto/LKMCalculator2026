# plan2akz — основной план развития АКЗ-калькулятора v3

> **Живой план проекта.** После каждого существенного этапа статусы обновляются прямо здесь.
>
> **Главный приоритет продукта:** **расчёт АКЗ → система покрытия → сравнение → Excel/PDF**.
>
> **Ключевое требование:** система покрытия должна поддерживать **2, 3, 4 и любое разумное количество слоёв**. Ограничение старого Excel-шаблона на 2 слоя не является ограничением калькулятора v3.
>
> Все дополнительные функции развиваются вокруг этого workflow и не должны его ломать.

## 0. Правила

Статусы:
- **[НЕ ВЫПОЛНЕНО]** — ещё не реализовано.
- **[ЧАСТИЧНО]** — реализована только часть.
- **[ВЫПОЛНЕНО, НО НУЖНО...]** — основная часть готова, есть конкретный остаток.
- **[ВЫПОЛНЕНО]** — реализовано и проверено.
- **[ЗАБЛОКИРОВАНО]** — нужен другой этап/решение.
- **[ОТЛОЖЕНО]** — сознательно перенесено.

Для каждого изменения:
1. не ломать рабочий АКЗ-расчёт;
2. расчётная логика остаётся в Domain/Service, не в UI;
3. Excel не становится вторым расчётным движком;
4. PDF и Excel получают данные из одного `CalculationResult`;
5. инженерные и складские данные не смешиваются без необходимости;
6. тест/smoke-check;
7. отдельный commit;
8. после commit обновляется `plan2akz.md`.

---

# I. ОСНОВНОЙ WORKFLOW АКЗ — КРИТИЧЕСКИЙ ПРИОРИТЕТ

## 1. Расчёт АКЗ

**[ВЫПОЛНЕНО, НО НУЖНО ПРОДОЛЖИТЬ]**

Уже есть: расчёт слоёв, DFT/WFT, теоретический/практический расход, кг/м² и л/м², разбавитель, стоимость системы, площадь и итоги, `CalculationResult`, несколько баз разбавления, валидация и отсутствие промежуточного округления в основном движке.

Осталось: precision/regression tests, полный контроль единиц, стабильный 2К pipeline, технологические проверки и прозрачный источник инженерных результатов.

**Обязательное требование:** расчёт должен работать не только для 2 слоёв, а для **3+ слоёв без потери точности и данных**.

## 2. Система покрытия на основном экране

**[ЧАСТИЧНО]**

Целевой workflow: `Расчёт → сформировать/получить систему → проверить → сравнить → экспортировать`.

Система должна быть **динамической по количеству слоёв**: минимум 2, 3, 4 и далее без архитектурного ограничения вида `layer1/layer2`.

Осталось:
- полноценная модель/UX системы;
- add/remove/reorder слоёв;
- 2+ слоя в одном расчёте;
- save/load/copy system с сохранением всех слоёв и их порядка;
- удобное получение системы из расчёта;
- one-click добавление результата в сравнение;
- минимизация переходов в БД;
- запрет silent truncation при любом количестве слоёв.

## 3. Материал из экрана расчёта

**[ВЫПОЛНЕНО, НО НУЖНО ДОПРОВЕРИТЬ]**

Сделано: `+ Материал` из расчёта, запись в БД, автоматическое добавление слоем в текущий расчёт, обновление каталогов, duplicate check по нормализованному имени и добавление существующего материала при дубле с информационным предупреждением.

Осталось: regression test duplicate normalization, проверка всех полей, перезапуск/сохранность, проверка систем, сравнения и экспорта.

## 4. Сравнение систем АКЗ

**[ВЫПОЛНЕНО, НО ПОКА НУЖНО РАСШИРИТЬ]**

Есть базовое сравнение и инженерный приоритет над ценой.

Осталось:
- корректное сравнение систем из 2, 3, 4+ слоёв;
- 2К;
- технологические показатели;
- объяснение различий;
- UX добавления результата из расчёта;
- regression tests;
- проверка, что система не обрезается до двух слоёв.

---

# II. EXCEL / PDF — ОСНОВНОЙ ВНЕШНИЙ РЕЗУЛЬТАТ

## 5. Excel — рабочий customer-facing экспорт

**[ВЫПОЛНЕНО, НО НУЖНО ДОПРОВЕРИТЬ]**

`КалькуляторЭКСЕЛЛЬ.xlsx` признан **реальным рабочим документом**, а не обычным шаблоном. Произвольный лист `Расчёт` не может считаться заменой.

**Новое решение v3:** customer-facing экспорт теперь содержит **одну таблицу**, без дублирования блока «Если стоимость в КГ» / «Если стоимость в литрах». В одной таблице сохраняются инженерные расходные показатели и оба ценовых поля (руб/кг и руб/л), поэтому пользователь может работать с материалами независимо от того, какая форма цены заполнена.

**Важно:** ограничение исходного `База` на 2 слоя относится только к историческому оформлению Excel. Экспорт v3 обязан поддерживать **2, 3, 4 и более слоёв** и не имеет права молча отбрасывать слои.

### 5.1. Аудит рабочего Excel

**[ВЫПОЛНЕНО]**

Проверен файл `КалькуляторЭКСЕЛЛЬ.xlsx`.

Фактическая структура исходника:
- 1 лист: `База`;
- 25 строк × 21 колонка;
- основной диапазон `B:R`;
- две исторические секции: `B1:R11` и `B15:R25`;
- каждая секция исторически рассчитана на 2 слоя + 2 строки разбавителя + итог;
- имеются merged cells;
- формулы находятся непосредственно в старом документе;
- скрытых строк/колонок нет;
- print area не задана; portrait;
- исходный файл содержит полноценное форматирование, числовые форматы и клиентскую форму.

**Критический вывод:** историческое дублирование kg/l больше не переносится в новый customer-facing результат. На выходе используется только первый визуальный блок `База`, расширяемый для 3+ слоёв.

### 5.2. Excel Mapping Specification

**[ВЫПОЛНЕНО]**

Создан документ `docs/plan2akz_excel_mapping.md`.

Зафиксирована карта `CalculationResult → лист/ячейка → единица/формат → правило заполнения`, включая поля B:R, итоги, строки разбавителя, kg/l секции, Engineering/Commercial/Full и ограничение legacy-шаблона на 2 слоя.

Зафиксировано обязательное правило: Excel получает значения из `CalculationResult`, а не пересчитывает их.

Стратегия v3 обновлена: customer-facing `База` использует один блок; исторический второй kg/l блок удаляется из результата. Инженерный экспорт является отдельным форматом и также не дублирует kg/l таблицы.

Commit документа: `2851063adc2412a218dc6a997514dd2f662cbdbe`.

### 5.3. Multi-layer Excel Exporter

**[ВЫПОЛНЕНО, НО НУЖНО ПРОДОЛЖИТЬ ПРОВЕРКУ]**

Реализовано в `CustomerExcelExporter`:
- прямое заполнение реального листа `База`;
- **одна таблица вместо двух исторических kg/l блоков**;
- поддержка 2, 3, 4+ слоёв через динамическое расширение;
- отдельные строки разбавителя для каждого слоя;
- перенос итоговой строки вниз без потери данных;
- восстановление merged cells после `insert_rows()`;
- копирование стилей, границ, форматов и высоты строк для добавленных строк;
- значения берутся из `CalculationResult`, а Excel не используется как расчётный движок;
- Commercial/Full выводят доступные ценовые поля; Engineering не смешивается с customer-facing legacy-формой.

Последние изменения:
- `5f3b31b59b14b994d913f4b131e788ee68000e34` — customer Excel переведён на один блок;
- `75fd682215bc083712522a7738988a1d54f7003c` — customer regression tests обновлены под одну таблицу;
- `ce7c3e63846a8b0d3bb6ea60ffcb6e36721cade8` — инженерный экспорт переведён на новый двухтабличный формат;
- `7593259bc72820a4050e13220cb844f36688f677` — режим `engineering` направлен в отдельный инженерный экспорт;
- `8fae6fb42a443549ededae73ec7a955d79aaeabb` — исправлено создание инженерного экспортера без передачи неподдерживаемых settings;
- `6a35542810224b1154910802e2a5c73c22b27adf` — добавлены regression tests инженерного Excel;
- `9950cbdb766cdf22f263a9818fae13a5195a605c` — исправлено сохранение заливок merged строк разбавителя через persisted top-left anchors;
- `e0688c8969044421ae6bc758acf1005fa97bca66` — исправлен golden-тест длинного наименования: убран случайный хвостовой пробел из тестовой строки.

### 5.4. Проверка Excel

**[ЧАСТИЧНО]**

Customer-facing regression tests покрывают:
- 2 слоя;
- 3 слоя;
- 4 слоя;
- расширение единственной таблицы;
- итоговую строку;
- сохранение стилей;
- отсутствие silent truncation;
- несколько/смешанные разбавители;
- длинное наименование материала без усечения значения;
- режимы Commercial и Full;
- отдельный Engineering export без цен материалов.

Инженерный экспорт имеет отдельный тестовый контур для 2/3/4+ слоёв и проверки состава данных без цен материалов.

**Инженерный Excel v3:** один лист `Инженерный расчёт` содержит две таблицы:
1. **Состав и расчёт системы** — слой, материал, связующее, DFT/WFT, теоретический/практический расход, разбавитель, потери, стоимость слоя;
2. **Итог системы** — количество слоёв, общая толщина, общий расход ЛКМ, общий расход разбавителя, стоимость 1 м², стоимость объекта, площадь.

Инженерный экспорт не выводит складские/закупочные поля и не содержит дублей kg/l секций.

CI `34388435594` на commit `9950cbdb766cdf22f263a9818fae13a5195a605c` завершился **success**: job `pytest` выполнен без ошибок. Предыдущий failure на merged-cell заливке устранён корректным использованием top-left anchor.

После зелёного CI добавлен commit `fbec87398dc1907f2427f0a8f2a1422fccce4668` с расширением golden/regression coverage: длинное наименование материала, Commercial/Full и Engineering, а также сохранена проверка смешанных разбавителей.

CI `34389129166` на commit `e0688c8969044421ae6bc758acf1005fa97bca66` завершился **success**: 178 тестов прошли без ошибок. Исправление касалось только тестовой golden-фикстуры, которая содержала лишний пробел в конце длинного наименования.

Добавлен следующий smoke-контур:
- `3f13664ed8c986965de6ec1e19f4a5dc82f2b78c` — тест headless-конвертации customer Excel → PDF через LibreOffice;
- `e0a537e34d22513e89d71a851690cacc25c92ed8` — CI устанавливает LibreOffice Calc и запускает полный suite вместе с новым smoke-тестом.

На момент обновления плана CI `34389186946` для `e0a537e34d22513e89d71a851690cacc25c92ed8` находится **в процессе выполнения**; итог пока не фиксируется как green.

Осталось:
- дождаться и зафиксировать зелёный CI с LibreOffice;
- проверить печать/PDF conversion не только на факт создания PDF, но и визуально;
- визуально проверить сохранение клиентского оформления `База`;
- проверить, что режим `Инженерный` в меню Excel выдаёт именно новый двухтабличный формат;
- проверить отсутствующие цены кг/л без изменения структуры customer-facing таблицы;
- проверить 2/3/4+ слоя при реальной печати/конвертации.

## 6. PDF — клиентский экспорт

**[ВЫПОЛНЕНО, НО НУЖНО ПРОВЕРИТЬ]**

Есть: Engineering/Commercial/Full, настройки организации, логотип/контакты, выбор режима перед экспортом, галочка текущего режима, A4 widths.

Осталось:
- реальный `CalculationResult`;
- визуальная проверка всех режимов;
- **2, 3, 4+ слоя**;
- длинные названия;
- переносы;
- исключение ошибочных `—`;
- legacy Excel → PDF workflow после восстановления формы Excel;
- совпадение состава системы Excel ↔ PDF.

---

# III. РАСЧЁТНЫЙ ДВИЖОК

## 7. Точность / единицы / regression

**[ЧАСТИЧНО]**

Нужно: DFT/WFT tests, dilution tests, theoretical/practical consumption, cost, area, systems, **multi-layer golden tests (2/3/4+)**, отсутствие промежуточного округления и единый контроль единиц.

## 8. 2К материалы

**[НЕ ВЫПОЛНЕНО]**

Использовать существующие `MaterialComponent`, `MaterialMix`, `Package`. Результат отдельно показывает A/B, соотношение, потребность, комплекты и стоимость. Не превращать приложение в ERP.

2К должен корректно работать как один или несколько компонентов внутри любого слоя и не ломать multi-layer pipeline.

## 9. Фасовка / закупка

**[НЕ ВЫПОЛНЕНО]**

Отдельный коммерческий слой: фасовка, количество банок/комплектов, остаток, резерв, закупочная стоимость. Инженерный расчёт не должен зависеть от склада.

---

# IV. БД / ИСТОРИЯ

## 10. Миграции БД

**[ЧАСТИЧНО]**

Нужна additive migration strategy через Alembic с backup, сохранением SQLite и rollback strategy.

## 11. История / snapshots

**[ЧАСТИЧНО]**

Использовать `CalculationSnapshot` для входов, системы, материалов, результата, версии формул/калькулятора и времени.

Snapshot обязан сохранять **полный список слоёв и их порядок**, а не только первые два.

---

# V. ИНЖЕНЕРНЫЕ ДАННЫЕ АКЗ

## 12. Среда / ISO 12944 / ГОСТ

**[НЕ ВЫПОЛНЕНО]**

Добавить версионируемую нормативную модель: ISO 12944 C1–C5/CX, Im1–Im4, отдельные условия по ГОСТ 9.104, стандарт/редакция/дата действия и traceability требований.

## 13. Подготовка поверхности

**[НЕ ВЫПОЛНЕНО]**

Sa/St, степень подготовки, шероховатость/профиль, ГОСТ 9.402 / ISO 8501 / ISO 8503.

## 14. Технологические условия

**[НЕ ВЫПОЛНЕНО]**

RH, температура воздуха/металла, точка росы, окно нанесения, фактический DFT, межслойная совместимость.

## 15. Огнезащита OGZ

**[ОТЛОЖЕНО]**

ПТМ, section factor, R, критическая температура и проверка огнезащитной системы — после стабилизации АКЗ core.

---

# VI. RECOMMENDATION / QUALITY

## 16. Recommendation Engine

**[ЧАСТИЧНО]**

Hard filters уже отделены от score.

Нужно: химическая среда, технология, совместимость, объяснение результата, configurable weights.

Recommendation должен возвращать полноценную систему из **2+ слоёв**, а не фиксированное количество.

## 17. Контроль качества / фактический DFT

**[НЕ ВЫПОЛНЕНО]**

Измерения DFT, контроль зон, recoat/ремонт, сравнение факта с проектом.

---

# VII. ФИНАЛИЗАЦИЯ

## 18. Release / regression / документация

**[НЕ ВЫПОЛНЕНО]**

Перед релизом: полный regression suite, smoke-check приложения, golden АКЗ cases для 2/3/4+ слоёв, Excel/PDF golden cases для 2/3/4+ слоёв, проверка БД, backup/restore, документация формул, нормативной базы и release checklist.

---

# ТЕКУЩАЯ ТОЧКА РАБОТЫ

**Сейчас выполняем: `5.4 Проверка Excel`.**

Последовательность без перескоков:
1. исправить все падения текущего CI;
2. получить зелёный полный pytest suite;
3. golden Excel cases 2/3/4+ и несколько разбавителей;
4. длинные названия и Engineering/Commercial/Full;
5. проверка Excel/LibreOffice и печати/PDF conversion;
6. затем проверка PDF на том же `CalculationResult`;
7. после стабилизации Excel/PDF — полноценная проверка сравнения систем;
8. обновлять статусы этого файла после каждого закрытого этапа.

**Текущий Excel-дизайн зафиксирован:** customer-facing экспорт — **одна таблица**, инженерный экспорт — **две таблицы на одном листе**. Оба формата поддерживают 2/3/4+ слоя и получают данные из одного `CalculationResult`.

**Главное правило:** пока цепочка `АКЗ → система → сравнение → Excel/PDF` не стабилизирована, второстепенные функции не получают приоритет над ней.
