# plan2akz — основной план развития АКЗ-калькулятора v3

> **Живой план проекта.** После каждого существенного этапа статусы обновляются прямо здесь.
>
> **Главный приоритет продукта:** **расчёт АКЗ → система покрытия → сравнение → Excel/PDF**.
>
> Все дополнительные функции развиваются вокруг этого workflow и не должны его ломать.

## 0. Правила

Статусы:
- **[НЕ ВЫПОЛНЕНО]** — ещё не реализовано.
- **[ЧАСТИЧНО]** — реализована только часть.
- **[ВЫПОЛНЕНО, НО НУЖНО...]** — основная часть готова, есть конкретный остаток.
- **[ВЫПОЛНЕНО]** — реализовано и проверено.
- **[ЗАБЛОКИРОВАНО]** — нужен другой этап/решение.
- **[ОТЛОЖЕНО]** — сознательно перенесено.

Для каждого изменения:
1. не ломать рабочий АКЗ-расчёт;
2. расчётная логика остаётся в Domain/Service, не в UI;
3. Excel не становится вторым расчётным движком;
4. PDF и Excel получают данные из одного `CalculationResult`;
5. инженерные и складские данные не смешиваются без необходимости;
6. тест/smoke-check;
7. отдельный commit;
8. после commit обновляется `plan2akz.md`.

---

# I. ОСНОВНОЙ WORKFLOW АКЗ — КРИТИЧЕСКИЙ ПРИОРИТЕТ

## 1. Расчёт АКЗ

**[ВЫПОЛНЕНО, НО НУЖНО ПРОДОЛЖИТЬ]**

Уже есть: расчёт слоёв, DFT/WFT, теоретический/практический расход, кг/м² и л/м², разбавитель, стоимость системы, площадь и итоги, `CalculationResult`, несколько баз разбавления, валидация и отсутствие промежуточного округления в основном движке.

Осталось: precision/regression tests, полный контроль единиц, стабильный 2К pipeline, технологические проверки и прозрачный источник инженерных результатов.

## 2. Система покрытия на основном экране

**[ЧАСТИЧНО]**

Целевой workflow: `Расчёт → сформировать/получить систему → проверить → сравнить → экспортировать`.

Осталось: полноценная модель/UX системы, add/remove/reorder слоёв, save/load/copy system, удобное получение системы из расчёта, one-click добавление результата в сравнение и минимизация переходов в БД.

## 3. Материал из экрана расчёта

**[ВЫПОЛНЕНО, НО НУЖНО ДОПРОВЕРИТЬ]**

Сделано: `+ Материал` из расчёта, запись в БД, автоматическое добавление слоем в текущий расчёт, обновление каталогов, duplicate check по нормализованному имени и добавление существующего материала при дубле с информационным предупреждением.

Осталось: regression test duplicate normalization, проверка всех полей, перезапуск/сохранность, проверка систем, сравнения и экспорта.

## 4. Сравнение систем АКЗ

**[ВЫПОЛНЕНО, НО НУЖНО РАСШИРИТЬ]**

Есть базовое сравнение и инженерный приоритет над ценой.

Осталось: 2К, технологические показатели, объяснение различий, UX добавления результата из расчёта и regression tests.

---

# II. EXCEL / PDF — ОСНОВНОЙ ВНЕШНИЙ РЕЗУЛЬТАТ

## 5. Excel — рабочий customer-facing экспорт

**[ЧАСТИЧНО — КРИТИЧЕСКАЯ ЗАДАЧА]**

`КалькуляторЭКСЕЛЛЬ.xlsx` признан **реальным рабочим документом**, а не обычным шаблоном. Произвольный лист `Расчёт` не может считаться заменой.

### 5.1. Аудит рабочего Excel

**[ВЫПОЛНЕНО]**

Проверен файл `КалькуляторЭКСЕЛЛЬ.xlsx`.

Фактическая структура:
- 1 лист: `База`;
- 25 строк × 21 колонка;
- основной диапазон `B:R`;
- две секции: `B1:R11` — стоимость в кг; `B15:R25` — стоимость в литрах;
- каждая секция исторически рассчитана на 2 слоя + 2 строки разбавителя + итог;
- имеются merged cells;
- формулы находятся непосредственно в старом документе;
- скрытых строк/колонок нет;
- print area не задана; portrait;
- исходный файл содержит полноценное форматирование, числовые форматы и клиентскую форму.

**Критический вывод:** Excel нельзя просто дополнять отдельным листом и считать задачу выполненной. Нужно заполнять существующую `База`, сохраняя её визуальную форму.

### 5.2. Excel Mapping Specification

**[ВЫПОЛНЕНО]**

Создан документ `docs/plan2akz_excel_mapping.md`.

Зафиксирована карта `CalculationResult → лист/ячейка → единица/формат → правило заполнения`, включая поля B:R, итоги, строки разбавителя, kg/l секции, Engineering/Commercial/Full и ограничение legacy-шаблона на 2 слоя.

Зафиксировано обязательное правило: Excel получает значения из `CalculationResult`, а не пересчитывает их.

Commit: `2851063adc2412a218dc6a997514dd2f662cbdbe`.

### 5.3. Legacy-compatible Excel Exporter

**[ЧАСТИЧНО — СЛЕДУЮЩАЯ ЗАДАЧА]**

Текущее состояние:
- `CustomerExcelExporter` загружает пользовательский шаблон;
- умеет искать отдельные подписи;
- создаёт дополнительный лист `Расчёт`.

Проблема: существующая `База` фактически не заполняется таблицей выбранных слоёв.

Следующая реализация:
1. прямое заполнение `База`;
2. сохранение стилей/merged cells;
3. заполнение обоих legacy-вариантов kg/l без повторного расчёта;
4. поддержка >2 слоёв через безопасное расширение строк;
5. вывод итогов из `CalculationResult`;
6. Engineering/Commercial/Full;
7. без складских данных в Engineering.

### 5.4. Проверка Excel

**[НЕ ВЫПОЛНЕНО]**

После реализации exporter: golden calculation, UI ↔ `CalculationResult` ↔ Excel, каждый слой, итоги, стили/форматы, Excel/LibreOffice, печать/PDF conversion.

## 6. PDF — клиентский экспорт

**[ВЫПОЛНЕНО, НО НУЖНО ПРОВЕРИТЬ]**

Есть: Engineering/Commercial/Full, настройки организации, логотип/контакты, выбор режима перед экспортом, галочка текущего режима, A4 widths.

Осталось: реальный `CalculationResult`, визуальная проверка всех режимов, длинные названия, переносы, исключение ошибочных `—`, legacy Excel → PDF workflow после восстановления формы Excel.

---

# III. РАСЧЁТНЫЙ ДВИЖОК

## 7. Точность / единицы / regression

**[ЧАСТИЧНО]**

Нужно: DFT/WFT tests, dilution tests, theoretical/practical consumption, cost, area, systems, golden tests, отсутствие промежуточного округления и единый контроль единиц.

## 8. 2К материалы

**[НЕ ВЫПОЛНЕНО]**

Использовать существующие `MaterialComponent`, `MaterialMix`, `Package`. Результат отдельно показывает A/B, соотношение, потребность, комплекты и стоимость. Не превращать приложение в ERP.

## 9. Фасовка / закупка

**[НЕ ВЫПОЛНЕНО]**

Отдельный коммерческий слой: фасовка, количество банок/комплектов, остаток, резерв, закупочная стоимость. Инженерный расчёт не должен зависеть от склада.

---

# IV. БД / ИСТОРИЯ

## 10. Миграции БД

**[ЧАСТИЧНО]**

Нужна additive migration strategy через Alembic с backup, сохранением SQLite и rollback strategy.

## 11. История / snapshots

**[ЧАСТИЧНО]**

Использовать `CalculationSnapshot` для входов, системы, материалов, результата, версии формул/калькулятора и времени.

---

# V. ИНЖЕНЕРНЫЕ ДАННЫЕ АКЗ

## 12. Среда / ISO 12944 / ГОСТ

**[НЕ ВЫПОЛНЕНО]**

Добавить версионируемую нормативную модель: ISO 12944 C1–C5/CX, Im1–Im4, отдельные условия по ГОСТ 9.104, стандарт/редакция/дата действия и traceability требований.

## 13. Подготовка поверхности

**[НЕ ВЫПОЛНЕНО]**

Sa/St, степень подготовки, шероховатость/профиль, ГОСТ 9.402 / ISO 8501 / ISO 8503.

## 14. Технологические условия

**[НЕ ВЫПОЛНЕНО]**

RH, температура воздуха/металла, точка росы, окно нанесения, фактический DFT, межслойная совместимость.

## 15. Огнезащита OGZ

**[ОТЛОЖЕНО]**

ПТМ, section factor, R, критическая температура и проверка огнезащитной системы — после стабилизации АКЗ core.

---

# VI. RECOMMENDATION / QUALITY

## 16. Recommendation Engine

**[ЧАСТИЧНО]**

Hard filters уже отделены от score.

Нужно: химическая среда, технология, совместимость, объяснение результата, configurable weights.

## 17. Контроль качества / фактический DFT

**[НЕ ВЫПОЛНЕНО]**

Измерения DFT, контроль зон, recoat/ремонт, сравнение факта с проектом.

---

# VII. ФИНАЛИЗАЦИЯ

## 18. Release / regression / документация

**[НЕ ВЫПОЛНЕНО]**

Перед релизом: полный regression suite, smoke-check приложения, golden АКЗ cases, Excel/PDF golden cases, проверка БД, backup/restore, документация формул, нормативной базы и release checklist.

---

# ТЕКУЩАЯ ТОЧКА РАБОТЫ

**Сейчас выполняем: `5.3 Legacy-compatible Excel Exporter`.**

Ближайший порядок:
1. заполнение реального листа `База` из `CalculationResult`;
2. поддержка 2 и более слоёв без потери данных;
3. golden test на рабочем Excel;
4. затем проверка PDF на том же результате;
5. обновление статусов `5.3`, `5.4`, `6`.

**Главное правило:** пока цепочка `АКЗ → система → сравнение → Excel/PDF` не стабилизирована, второстепенные функции не получают приоритет над ней.
