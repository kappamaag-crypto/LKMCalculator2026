# LKM Calculator v2.0 — статус загрузки

Часть файлов уже в репозитории (`v2/`).

**Полный исходник** находится в рабочей сессии:
`/home/workdir/artifacts/lkm_calculator/`

Из-за объёма (~50 файлов, ~250 KB кода) полная выгрузка через API идёт пакетами.

## Уже залито
- v2/README.md, v2/main.py, v2/requirements.txt
- v2/app/__init__.py
- v2/app/domain/enums.py (и обновляется)

## Как получить всё сразу
1. Скачать папку `lkm_calculator` из артефактов сессии
2. Или продолжить: напишите «продолжи пуш» — залью оставшиеся пакеты

## Запуск (когда все файлы на месте)
```bash
cd v2
pip install -r requirements.txt
python main.py
```
